#include <stdio.h>
#include <math.h>
#include <string.h>

char temp_str[256];
int heat = 0;
int door_closed = 1;
int door_open = 0;

int main() {
    int result = 42;
    int heat = 5;
    printf((sprintf(temp_str, "Arithmetic result: %d", result) ? temp_str : temp_str));
    heat = (heat + 1);
    return 0;
return 0;
}

